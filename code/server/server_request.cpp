#include <stdio.h>

#include "server_manager.h"
#include "server_request.h"

ServErr Kill()
{
    return SERV_CORRECT;
}

ServErr Test()
{
    return SERV_CORRECT;
}

ServErr Request(const char* buffer, size_t buffer_len)
{
    return SERV_CORRECT;
}